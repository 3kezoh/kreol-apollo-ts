const auth = require("./auth");
const definition = require("./definition");
const report = require("./report");
const user = require("./user");
const vote = require("./vote");

module.exports = [auth, definition, report, user, vote];
