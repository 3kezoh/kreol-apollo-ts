const count = require("./count");
const definitions = require("./definitions");
const definition = require("./definition");
const search = require("./search");

module.exports = { count, definitions, definition, search };
