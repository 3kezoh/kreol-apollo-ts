# kreol-apollo-ts

The Kreol Back-End

## Requirements

- Docker

## Usage

```bash
# Runs mongodb
docker-compose up -d
```

In addition you need to run the API (not dockerized)

```bash
# Runs API
npm install
npm run dev
```
